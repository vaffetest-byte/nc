# SPA Routing for GoDaddy
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Handle Front Controller - redirect all requests to index.html for SPA routing
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^ index.html [L]
</IfModule>
